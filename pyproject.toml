[project]
name = "supercode"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.48.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
